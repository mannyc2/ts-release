# The credentials the shipped recipes read by default. A config may name
# others through `tokenEnv`; npm trusted publishing (OIDC) needs neither of
# these — it uses the Action's id-token request variables instead.
GH_TOKEN=
NPM_TOKEN=
