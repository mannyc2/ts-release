# Architecture program

The consolidated, tracked execution handoff is
[`handoff/README.md`](handoff/README.md). It reconciles the recorded research,
retains the 69-outcome product scope, supplies concrete experiments and the exact
proposed API/layout/migration, and separates maintainer choices from implementation
and native acceptance gates. The original inputs remain immutable provenance.
