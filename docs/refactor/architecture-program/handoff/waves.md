> Current authority: [README](README.md), [design](design.json), and the matching
> JSON records include the accepted P0–P3 amendments. The discussion below
> preserves the earlier investigation; old topology/45-line proposals and
> research-only authorization are superseded. Native oracles remain required.

# Sequential implementation and acceptance handoff

[Waves JSON](waves.json) assigns every one of the **69 selected outcomes** to
exactly one implementation wave and preserves each original scope, native
oracle, fixture/destination, dependency, witness kind and scorecard line. W04 is
a mechanism prerequisite, not a seventieth product outcome. This packet plans
future execution; production refactoring and publication were not performed in
the research task.

Consume the agreed [design](design.json), [generated contract](contract.json),
[layout](layout.json) and [public surface](public-surface.json). These waves make
no topology, version or marginal-budget selection. An unresolved central field
must be resolved before product cutover. The qualified prototypes are concrete
algorithm/representation evidence; they are not implementations of all native
provider outcomes. [Qualification](qualification.md) preserves that boundary.

## Starting coordinate and source disposition

Use one clean sequential implementation line descended from PR21
`887a9fe2b35590f3088ffeee84f32722796e03ab`. Check that commit is an ancestor and
that PR22 `c2ac4ee4e7f02d74a7a1ff435bdfeaca6890b720` and preserved overlay
`2ef7a9a61fe40608d053569cbcd71e40fca5c181` are not ancestors. Exact argv and
expected exit codes are in JSON. Use the `codex/` branch prefix.

PR22/overlay are read-only evidence, never merge/cherry-pick or bulk-copy
sources. Translate their native protocol evidence into the selected owners;
remove replaced current representations/readers/writers/declarations in the
same wave. Historical deletion already performed receives no second credit.
The user authorized a hard cut and knows of no external consumers. Do not add
old-format aliases or reconstruct the old CLI command/flag surface.

[Migration](migration.json) owns exact current paths, hashes, dispositions and
successors. Producers belong to W08, MCP/OpenAI to W09, and CLI/Action/self-release
to W10. All old public symbols and formats keep explicit dispositions; moving
source does not reduce its measured cost.

## The accepted wire-first order

Each wave begins at its predecessor's exact terminal commit. There is no
parallel provider/producer convergence merge.

| Wave / executor role | Portfolio plan | Assigned selected IDs | Required implementation result |
| --- | --- | --- | --- |
| W01 kernel/local host | Plan 006 | K01 | Canonical schemas, identity, one M1 command owner/interpreter, complete DAG, captured host, global CAS, native evidence/error admission and report projection. |
| W02 npm vertical | Plan 007 | D01-01–D01-06 | Exact tarball/version/tag/provenance laws, three workspaces/private omission, native auth and loss recovery. |
| W03 Warehouse vertical | Plan 007 | D02-01–D02-07 | Per-file native uploads/metadata, trusted auth, Simple observations and distinct pinned compatible-server laws. |
| W04 native transports | Plan 007 | Mechanism prerequisite | Single native HTTP send/cancellation/bounds and credentials; owned Git object restoration, captured authority table and exact expected-old CAS. |
| W05 GitHub vertical | Plan 007 | D03-01–D03-06 | Tags/objects/refs, draft, independent assets and publish DAG; native returned IDs resolved from declared dependencies. |
| W06 catalog verticals | Plan 007 | D04-01–D04-03, D05-01–D05-03 | Four-cell Formula and two-cell Scoop rendering, exact immutable downloads and atomic one/two-path Git commits. |
| W07 external contract | Plan 007 | D06-01–D06-07 | Unknown external tarball after core/CLI build, strict heterogeneous/native codecs, two instances and process-2 continuation. |
| W08 artifacts/producers | Plan 008 | P01-01–04, P02-01–03, P03-01–02, P04-01, P05-01/02/03/05/06, P06-01, P07-01, P08-01, P09-01–03, P10-01–04, Q01, Q02-01–02 | All 28 selected producer/quality outcomes through exact effect-build packages and owned File/Tree/Bundle; one-journal Apple preparation and final adoption. |
| W09 late AI providers | Plan 008B | D07-01–D07-03, AI01–AI03 | Official MCP schemas/publication, skills-only package, marketplace CAS and strict human-submission handoff validation. |
| W10 hosts/self-release | Plan 008B | K02, K03 | Scoped real CLI/Action/library application wiring, explicit shared store, complete release continuation and non-mutating intended self-release rehearsal. |

Do not confuse D06, the **external-provider** family, with D07, the **MCP**
family; older portfolio text used an obsolete label. No P05-04, MSI, OCI,
nightly/version-derivation or other deferred outcome is introduced by a range.
The exact JSON ID set is authoritative.

W01 promotes the stable architecture obligations into independent production
contracts. It must carry C01/C02's explicit trace corrections and implement the
C08 endpoint-before-observation correspondence check. Native external-provider,
actual Apple and full producer integration witnesses remain assigned to W07/W08;
a generic kernel fixture is not their acceptance. W02–W07 prove native release
foundations before producer breadth. W08 completes producer/trust work before
W09/W10 late-provider and host convergence.

## Exact local gates and pass witnesses

The following are **future production suites to implement**, with actual
assertion bodies and independent native fixtures. Their directories/commands
are explicit handoff targets, not claims that these suites already exist or
pass. Existing research commands remain separately listed in qualification.

| Wave | Exact production suite command | Distinguishing pass witness |
| --- | --- | --- |
| W01 | `bun test ./test/reimplementation/kernel` | Legal/illegal history, all six event families, CAS-only authority, AlreadyRecorded, scoped risk, late facts, native errors, full DAG, request drift and real process replacement. |
| W02 | `bun test ./test/reimplementation/npm` | Independent native request bytes and version/tag observations; strict provenance/native errors; three-public and two-public/one-private cases; interruption never repeats unknown mutation. |
| W03 | `bun test ./test/reimplementation/warehouse` | Exact two/four-file progress; native metadata; pinned pypiserver/devpi install/import/console-script; absent Simple evidence never inherits a replay law. |
| W04 | `bun test ./test/reimplementation/transports` | Actual peer write count on loss/cancel; fd/socket/temporary-resource closure; two Git authorities after deleting build repositories; exact native CAS and secret/endpoint binding. |
| W05 | `bun test ./test/reimplementation/github` | Invalid complete DAG causes zero effects; zero/three assets; pagination/starter/name/digest; returned parent ID survives restart and wrong parent ID prevents send. |
| W06 | `bun test ./test/reimplementation/catalog` | Formula/Scoop selector/hash/native rendering oracles; real Git atomic one/two-file commits preserve unrelated paths and reconcile lost acknowledgment. |
| W07 | `bun test ./test/reimplementation/external` | Actual clean Node/Bun tarball consumers, provider built after CLI, no source registry edits, exact native errors and fresh-process opaque/core continuation. |
| W08 | `bun test ./test/reimplementation/artifacts` | Every original P/Q tool/format/bytes oracle, real installed producer package, strict owned restore, native platform fixtures, same physical Apple journal and exact final-byte binding. |
| W09 | `bun test ./test/reimplementation/ai` | Official MCP package/transport matrix, native recovery, installable skills package, marketplace CAS, five positive/three negative handoff fixtures; no portal-success fiction. |
| W10 | `bun test ./test/reimplementation/hosts` and `bun test ./test/reimplementation/self-release` | Actual bundles/default application wiring, lifecycle on success/failure/interruption, complete release process replacement and full intended-byte self-release with dispatch disabled. |

Each suite emits stable `acceptance.<selected-id>` case identities with exact
source/package/fixture and executed assertion results. JSON includes the
**original oracle text for every selected ID**, not a paraphrased substitute.
For example, D02-03 is one sdist plus the three specifically named wheels;
D03-03 demonstrates zero and three assets; P10-01 durably retains the actual
submission identity; AI03 validates a handoff and does not submit it.

Every wave also runs/adapts these existing or prescribed checks against the
selected new contract:

```text
bun run check:architecture-program
bun run check:import-rules
bun run check:package-exports
bun run check
git diff --check
```

`check:architecture-program` is a prescribed successor gate, not a claim that
the broken historical selector is ready for production acceptance. Create/adapt
missing entrypoints before relying on them; keep concrete failures red until
implementation satisfies their law. Reuse appropriate existing checkers while
removing old layout/CLI assumptions. A checker that compares its own generated
expected result to itself is not an independent native oracle.

## Local convergence, live acceptance and certification

Plan 008B terminates at one local candidate binding exact source/tree ancestry,
central contract hashes, package/tarball coordinates, Action bundle, intended
self-release bytes, emitted surfaces/graphs, executed cases, migration and
budget totals, and the remaining native/hosted obligations. It is a buildable,
reviewable input to Plan 009, not a publication authorization.

Plan 009 then supplies the actual missing authority appropriate to each
original row. Construct a concrete matrix of disposable package/repository
coordinates, account/principal/scope, workflows/hosts, credentials, shared-store
policy/retention and intended effects before any live mutation. The current
research task grants no such mutation authority. Validators are exactly:

```text
bun run check:launch-evidence
bun run check:launch-closure
```

These prescribed commands must be implemented/adapted to require exactly
69 unique selected IDs and every original witness facet. They must reject
skipped tests, invented case IDs, stale sources, free-text-only receipts and
using a local double to satisfy a hosted/native oracle. Required work includes
real npm/PyPI/trusted publishing, GitHub/catalog/MCP outcomes, platform/native
producer consumers, credentialed signing/Apple where selected, actual Action
shared-store policy and non-manual self-release. Scope and limits come from
each original row; not every local provider contract requires a live account.

The immutable upstream npm-only amendment defers upstream Apple/AWS release
certification and removes that obsolete import prerequisite. It does not remove
ts-release's P06–P10, K02/K03 or any of its other selected rows. Eventual S3 or
Git deployment qualification applies to the actual configured host; no generic
S3 requirement or automatic Git-ref Action default is introduced here.

Plan 010 independently checks the exact terminal candidate and all 69 closure
records, rebuilds packed bytes/surfaces/graphs and host deliveries, challenges
native correspondence and uncertainty, and validates the full source/cost
ledger. Its gate vector includes architecture, core, packed consumers, evidence,
closure and `git diff --check`. Any product change invalidates affected
exact-coordinate evidence and returns to the responsible implementation wave.
A certificate cannot be generated from this research lab's passing test count.

## Source and maintenance accounting

The unchanged product-source ceiling is **11,485 lines** against the preserved
22,971-line denominator. Enforce the original counting lanes with all current
product source, generated product inputs, Action source, relocations and newly
maintained external implementation cost charged. Report tests/oracles, fixtures,
generators/tooling, documentation and generated/delivery output separately in
the complete maintenance ledger. Externalizing code or counting generated
output as deleted source cannot create a pass.

Measure actual complete implementation and real marginal patches, including
manifests/exports and recovery/migration support. The exact marginal policy is
read from the agreed central design; qualification retains the original
nonzero-population and 40-line median conflicts. The common-core and provider
forecasts help plan work, but neither a forecast nor the 105-line M1-specific
file satisfies the complete-product source gate.

The [published 0.3.0 inventory](public-history-inventory.json) also covers the
old operation-journal exports absent from the current branch. W04 may translate
its concrete AWS host/auth obligations if the optional host is selected; it
must use the new global JournalStore and must not wrap the old five-record,
rebasing CanonicalOperationJournal. Every maintained translation is charged;
the historical source adds no denominator or deletion credit. The archive
provides no new AWS deployment pass or upstream readiness requirement.

The [migration summary](migration.json) binds its complete `migration.json.gz`
payload. Use `bun tools/architecture-lab/records.ts
docs/refactor/architecture-program/handoff/migration.json` to read all original
rows, or append `--check` to verify the hashes without printing them. Compression
changes no execution owner, source disposition, witness or budget denominator.
