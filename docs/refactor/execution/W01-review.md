# Independent bounded W01 cutover review

Reviewer: Codex agent `review_beta_declaration` (Hypatia), independent of the
implementation author. Review conducted2026-09-07 on the actual working source.
This is not full release-candidate, selected-outcome or publication certification.

The reviewer independently reproduced and checked mutable request/event/snapshot,
codec, native tree/symlink/FIFO, observation-routing and evaluator-report defects
and reviewed their corrections in successive bounded passes. The final application
pass found a retained mutable host table; capture now owns the provider/configuration
collections and binds stable service functions before asynchronous admission.
A follow-up found the Git adapter's private class receiver was lost; execute is
now bound to its original receiver, with a private-state/method-replacement test.
The last reviewed Git suite passed7 tests/57 assertions; no remaining recovery
issue was identified in the bounded kernel/application cutover.

Migration review independently matched all51 retired paths to the original W01
assignment and source SHA-256 values:10318 physical lines. It also reconstructed
TypeScript AST nodes and verified all876 retired declaration hashes. The reviewer
found that resolve.ts native validation needed explicit carry-forward routing;
W01-migration.json now assigns its npm/PyPI/source/application duties to
W02/W03/W08/W10. No future native implementation is represented as already passed.

The review requested wiring check:migration into a normal gate and correcting a
stale forecast policy sentence. Both are addressed. Historical package inventory
now compares its recorded "current" snapshot to immutable9b14c6c, preserving the
exact old census while a separate migration check verifies actual deletions.
The old delivery script/build configuration remains an explicit W10 cleanup duty.

The reviewer permits progression into W02 with the recorded2779-versus1700 planning
variance and named open native duties. The raw budget comparison remains failed.
The accounting gate may admit only the exact source digest bound in
W01-source-variance.json; this cannot waive or pass the final11485 ceiling or the
40-line marginal target. Full-product forecast12525 remains1040 above the ceiling.

C08 is a configured public preflight seam witness using the actual shared prepared
request. Production native provider/host observation binding remains a later-wave
acceptance obligation, not an inferred sandbox property of arbitrary provider code.
