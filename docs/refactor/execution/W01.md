# W01 kernel cutover — locally verified with explicit planning variance

The approved kernel package uses aligned Effect beta.107 and the exact owned
Sentinel declaration repair. Original workspace/index and `.repos/effect` remain
untouched. Preservation checkpoint: `7da2877`; prerequisites: `65f65b1`;
tested W01 progress checkpoint: `8fe9891` (pushed).

The production kernel owns immutable Plan/Journal/request admission, core history
and append laws, one interpreter/send boundary, fresh CAS authorization and safe
response-loss/restart. M1 is default; external M2/M3/cache witnesses use public
contracts. Full reports derive from canonical facts even with a custom evaluator.
Scoped application loading now requires the owned Bundle, validates its Plan
binding before dispatch, and returns a public FinalizedReport Schema containing
Bundle, publication/preparation Plans, global Journal and operation status.

Application and direct kernel calls capture host functions/configuration before
asynchronous admission. Ports preserve their receiver state behind bound functions;
provider tables/scopes and durable inputs are immutable. Caller aliases cannot
replace transport, provider definitions, clock or authorization during an invocation.
Core Git transport identity is frozen and its native adapter/fallback captured.

Owned content streams/verifies distinct bytes, rejects symlinks and special files,
and uses exclusive temporary files, fsync/link/readback. Bundle finalization/loading
share native ordered tree identity and graph admission. Scoped Bun SQLite implements
global CAS, exact event-ID readback and symmetric1 MiB event bounds.

## Evidence and limits

- Strict source/test check and fresh runtime/declaration builds pass.
- Final full suite:122 tests,792 assertions across11 files.
- Actual local HTTP/Git/SQLite, OS-process replacement, exact1 MiB/+1 bounds,
  native effect-build File/Tree finalizers and FIFO liveness regressions pass.
- C08 configured public provider preflight rejects request fingerprint and
  endpoint/principal/scope drift before any observation or dispatch; a matching
  control succeeds. This is a seam witness, not native provider observation I/O
  certification or a claim to have replayed literal obsolete harness IDs.
- Public API projection now resolves actual emitted kernel/application owners;
  remaining provider/HTTP/adoption proposals stay explicitly unimplemented.
- Fresh Bun/npm installed tarballs passed public root/bundle/http/bun/node subsets,
  strict declarations and Sentinel-removal negative controls, optional-peer absence,
  Node22.22.2/Bun and scoped application/full-report behavior. Final host-capture tarball evidence is bound in packed-kernel.json.
- Bounded independent reviews corrected mutable alias, native evidence, tree,
  special-file, report fabrication and host-capture issues. Bounded cutover review passed; whole release-candidate review remains required.
- All69 launch rows remain open. No complete cohort, CLI/Action, hosted/native
  provider or published-consumer acceptance is claimed.

## Migration and measured source

[W01-migration.json](W01-migration.json) binds all51 retired files/declarations to
original hashes, their replacement owners, and explicit mixed native responsibilities
carried to W02/3/4/5/6/8/9/10. The10318 original physical lines are actually removed.
Original code remains in Git; no maintained copy is hidden in tooling. Remaining
legacy src files are unshipped donors until their assigned waves, not a second
working release runtime. Migration validation requires their later native duties
to be verified before W10 completion.

Current product: **10737 physical lines =2779 replacement +7958 legacy**.
This incomplete-product count is not an achieved source-reduction result.
The original W01 planning tripwire1700 is exceeded by1079. The measured kernel
allocation is2160; mixed actual/future full-product forecast12525 exceeds the
unchanged11485 ceiling by1040. Future provider/host/producers remain estimates.

Two simplification passes removed redundant failure handling/copies and centralized
observation/report ownership. The first retained count saved53 lines. No isolated
net count was retained for the second; required admission/report/host corrections
were added concurrently and are fully charged. W01-source-variance.json records the exact reviewed implementation digest;
neither deletion alone nor a variance establishes
complete-product success. Preserve the40-line marginal target and22971 baseline.

## Next action

Commit the verified cutover, then implement W02 npm on that ancestry, carrying
its exact native duties above.
Full Plans009/010 certification and actual publication remain ahead.
