# W01 progress checkpoint — not a completed wave

Implementation uses the approved kernel package, exact aligned Effect beta.107
pins, and the owned Sentinel declaration repair. Original workspace/index and
`.repos/effect` remain untouched. Prerequisite checkpoint: `65 f65 b1`.

The kernel admits immutable Plan/Journal/request data, verifies every append
against core laws, and grants a single send only to a fresh CAS winner or the
same live invocation's exact ambiguous-append readback. Status reports derive
from core history laws even with a custom evaluator. Observation results cannot
replace core routing. M1 is the default; M2/M3/cache witnesses use public contracts.

Owned content streams and verifies exact bytes, retains a distinct copy, rejects
symlinks and special files without blocking, and publishes immutable names through
exclusive temporary files with fsync/link/readback. Bundle finalization and loading
share ordered native tree identity and graph admission. The complete internal
report retains canonical Bundle, publication/preparation Plans and global Journal
facts; application/public report integration remains unfinished.

## Verification and limits

- `bun run check`: strict source/test check and fresh declaration/runtime build.
- `bun test test/reimplementation/kernel`:113 passed,742 assertions,9 files.
- Actual local HTTP/Git/SQLite and fresh OS-process continuation; exact1 MiB/+1
  SQLite profile, native File/Tree finalizers and FIFO liveness regression.
- `check:import-rules`: actual AST imports and runtime closure admission.
- `check:package-exports`: actual source/compiler/runtime surfaces;1 package,
  5 implemented entry subsets,39 pending requirements. `--complete` remains red.
- Packed consumer receipt: `packed-kernel.json`; fresh Bun/npm, Node22.22.2/Bun,
  optional peers absent, each entry strict without skipLibCheck, independent
  removal of the declaration repair fails as expected then restores exact bytes.
- Bounded independent reviews found no remaining issue in their reviewed
  corrections. These reviews do not certify a terminal release candidate.
- All69 launch outcomes remain open. No native platform, hosted release, CLI,
  Action, complete cohort or published-consumer acceptance is claimed.

## Source tradeoff — gate remains red

Current physical product: 20,905; replacement: 2,629; unchanged legacy:
18,276. W01's1,700 planning tripwire is exceeded by929 lines.
The complete-product11,485 ceiling and22,971 denominator are unchanged.

First simplification: redundant Effect failure wrappers and duplicate copying
removed, reducing the then-current slice from2,199 to 2,146 lines (53 lines).
Second simplification: one observation-recording helper, one core report
projection, preservation of already-owned Schema instances instead of JSON copies,
and removal of an always-false local permit flag. A concrete temporary measurement
for this pass alone was not retained; do not invent a net reduction. Required
Bundle/content/report code and review corrections were added concurrently and
are fully charged in the current count. The prior lab-density forecast does not
establish a normally formatted production budget pass.

51 unchanged W01 migration rows contain10,318 physical lines. They receive zero
credit until their replacements/callers are proven and the old files are actually
deleted. The remaining providers/producers/hosts must demonstrate their actual
cost; no scope cut, formatting trick, target increase or denominator change is
approved. Continue simplification and measured reconciliation across the full
selected implementation while preserving the red planning gate.

## Exact remaining W01 work

1. Regenerate/reconcile actual-owner declarations, vocabulary and design projections;
   current retained lab seals are not a production qualification mechanism.
2. Integrate the derived full report into the explicit application boundary and
   document the correction to the counts-only proposed host return contract.
3. Complete exact C08 before-observation endpoint/request mismatch evidence and
   bind stable production law IDs without pretending native W07/W08 checks passed.
4. Resolve all51 assigned migration rows and physically retire superseded
   constructors/readers/writers/public aliases, preserving needed native donor
   obligations for subsequent waves. Record hashes and real deletion arithmetic.
5. Review the concrete source tradeoff, complete successor gates and make the
   terminal W01 commit. W02 then implements the npm vertical on that ancestry.

This checkpoint is safe progress preservation. It is not W01 completion, Plan009/
010 certification, or a published release.
