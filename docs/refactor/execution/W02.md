# W02 npm native publication

W02 is locally implemented and independently reviewed, with an exact source
planning variance. It follows W01 commit `d214df5` on `codex/complete-hard-cut`.
The complete selected product, Plans009/010 and publication remain open.

`packages/npm` owns native tarball inspection, public operation authoring, exact
package PUT and dist-tag encoding, independent version/tag evidence, scoped token
and trusted-publisher exchanges, adopted provenance and actual Sigstore adapters.
Its real public declarations and dependency graph project from production owners.
Three public workspaces produce three uploads; private candidates are omitted.
Immutable hashes belong to PublishIntent, and native preparation recomputes them.
The complete body is admitted before credentials or receipt acceptance. Initial
prereleases require non-latest tags; a later explicit tag operation can move a tag.
After possible publication, absence or tag drift never creates resend permission.

The common transport now has optional invocation-local preparation. Credentials
can resolve before DispatchStarted. Only fresh conditional append authority can
invoke its captured send closure. CAS loss, AlreadyRecorded and restart discard
the closure; no persisted permit or credential cache exists. Missing, malformed
and aliased capabilities are tested. Core Git retains its protected identity and
forwards ordinary HTTP preparation without bypassing it.

Fresh local production suite: **148 tests / 1024 assertions across17 files**.
An actual local HTTP protocol double plus SQLite and SIGKILL after simulated npm
commit proves fresh-process absence/success/moved-tag observation with exactly
one send, one DispatchStarted and zero receipts. Native request goldens come from
unchanged official npm/libnpmpublish encoders with only the outbound port captured;
the full PUT comparison declares its JSON key-order and npm _nodeVersion differences.
The reviewer independently reproduced those encoders and confirmed their hashes.

Fresh kernel/npm archives install with Bun and npm. Strict declarations compile
without optional platform/Apple peers; installed files match the archives. Public
Node22.22.2/Bun consumers exercise provider preparation, execution and restart.
The actual candidate archives are also native-publication inputs, catching the
manifest registry URL's missing canonical trailing slash before any publication.
See [W02-packed-npm.json](W02-packed-npm.json).

An authentic existing public npm attestation passes actual Node Sigstore trust,
signature, certificate, SCT/transparency and source binding. Native signature,
certificate and well-formed Merkle-path corruptions reject. The source mismatch
control is explicitly statement admission, not an isolated certificate-extension
negative. Direct Bun1.3.14 TUF ECDSA verification fails on the authentic root; that
runtime remains unqualified for Sigstore. Live signing/OIDC and host qualification
remain open. See [W02-native-sigstore.json](W02-native-sigstore.json).

The assigned `src/publication/npm.ts` is physically retired: **525 lines and27
exact declaration hashes**, bound to the original source. Its old certified-child
execution model is gone; unsupported correction remains non-executable. No overlay
ancestry or deletion credit was taken. W01's npm-native duties now have successor
witnesses; actual source/producer/application disagreement checks remain W08/W10.
See [W02-migration.json](W02-migration.json) and [W02-review.md](W02-review.md).

Normally formatted physical product is **11832 =4399 replacement +7433 legacy**.
W02 adds1513 npm and107 core/HTTP lines; its1620 increment is below the1650 wave
allocation. The cumulative3350 planning tripwire still fails by1049, carrying W01's
overage. The full forecast is12451, exceeding the unchanged11485 ceiling by966.
No achieved reduction is claimed. Metadata, tests, textual fixtures, binary fixture
bytes, generated output and tooling are separate lanes; the22971 denominator and
40-line marginal target are unchanged. [W02-source-variance.json](W02-source-variance.json)
permits continued implementation only for the exact measured bytes.

Next: W03 Warehouse/PyPI per-file native publication, pinned compatible servers,
exact Simple evidence and two/four-file restart acceptance. The all69 launch rows,
whole seven-package consumers, CLI/Action/self-release and final independent
release certification remain open. No version has been published.
