# W03 — native Warehouse/PyPI cutover

The implementation follows W02 commit `fd4b8af`, which is pushed. W03 has local
native, consumer and independent review evidence; direct verification passes (257 sealed files, 226 propositions).
The preservation commit is the next checkpoint step. No release is published.
All 69 launch outcomes and Plans 009/010 remain open.

Implemented per-file wheel/sdist intents, complete selected-set admission,
bounded owned ZIP/TAR metadata inspection and reconstructed native multipart
uploads. Simple JSON/HTML provides exact filename/SHA-256/size/yanked evidence.
Token and trusted authorization bind endpoint, identity and the explicit normalized
project set. A response loss leaves uncertainty; fresh absence grants no resend.
The shared verified artifact reader and strict JSON grammar now have npm and
PyPI callers. Their owned lines moved to the kernel and are charged there.

The final production suite passes **159 tests / 1,379 assertions** across 21 files.
Actual pypiserver 2.4.1 and devpi-server 6.20.3 each pass two/four-file uploads,
SIGKILL after remote commit, fresh-process SQLite restart with temporarily hidden
Simple evidence, zero duplicate sends and clean pip install/import/console checks.
The TLS gateway forwards actual native bodies; only visibility is fault-injected.
The two platform wheels are retagged pure Python fixtures, not compiled extension
or platform-host acceptance. Native environment setup and exact Python dependency
pins are wired into the existing CI jobs; those remote runs remain to be observed.

Native Twine 7.0.0 supplies the multipart oracle and **82 metadata controls**, with
34 accepted cases. The independent reviewer regenerated these, verified upstream
hashes and the exact 699-license/79-exception SPDX catalog, and passed 3 tests /
196 assertions in its final bounded rerun. The extracted pinned Warehouse helper
and response branches prove exact-duplicate/different/deleted-filename distinctions
with explicit database/HTTP stand-ins. That is source-law evidence, not hosted
Warehouse acceptance. See [W03-review.md](W03-review.md).

[W03-packed-providers.json](W03-packed-providers.json) records fresh Bun/npm archive
installs for kernel, npm and PyPI, exact installed dist bytes, strict declarations,
optional peer absence and 20 Node/Bun consumer smokes. Installed PyPI executes all
82 native metadata controls under each runtime/manager. Its publication smoke
retains Pending when acknowledged files are temporarily invisible, then Satisfied
on exact Simple evidence, with four total sends. The initial smoke incorrectly
expected immediate visibility; correction changed the witness, not product recovery.
These are three of seven unpublished packages, not final release artifacts.

Both assigned old PyPI files are retired: **494 lines / 32 declarations**. Exact
original hashes and successor paths are in [W03-migration.json](W03-migration.json).
W01's carried local project/repository/identity/auth duties are covered. Common live
HTTP mechanics remain W04; hosted PyPI/TestPyPI/OIDC remains Plans 009/010.

Current physical product is **12,740 = 5,801 replacement + 6,939 legacy**. W03 adds
1,402 lines (1,378 PyPI + 131 kernel − 107 npm), exceeding its 1,150 allocation by
252. Cumulative overage is 1,301. The exact planning variance permits continued
work; it does not waive the unchanged **11,485** full-product ceiling. The mixed
full-scope forecast is 12,723, a 1,238-line deficit. Owned generated catalog data
adds **789 lines / 15,973 bytes**, with logic still counted TypeScript. Current
product plus catalog data is 13,529 lines. Metadata, tests, binary/text fixtures,
generated output and tooling remain separately visible. No achieved reduction.

Next: commit/push this verified checkpoint,
then implement W04 native HTTP credentials/redirects/timeouts and conditional Git
transports. Resolve direct Bun Sigstore verification through real native evidence.
Continue W04–W10, full acceptance, independent candidate certification, authorized
publication and fresh published consumers. The persistent goal stays active.
