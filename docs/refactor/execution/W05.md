# W05: GitHub provider cutover

W05 implements the six GitHub operations in the fourth public package. The shared
kernel admits complete provider graphs before I/O and binds native requests and
observations to declared dependency evidence. The provider uses the existing HTTP
host and common executor; it adds no release engine, history or dispatch authority.
Final wave integrity/accounting checks pass:276sealed files/226propositions. Nothing is published.
All69selected launch outcomes remain open; W06–W10 and Plans009/010 remain required.

Lightweight tags, annotated objects/refs, drafts, independent asset uploads and
publication have durable intent/fact codecs and exact native requests. Returned
object IDs, release IDs and upload templates are selected from validated preceding
history. Graph validation covers mandatory parents, repository/tag agreement,
duplicate coordinates and the complete selected asset set, including zero assets.
Draft preparation requires complete authenticated absence and exact tag target;
asset preparation checks the draft parent; publication checks the current complete
asset set and tag target. Optional observation never replaces these preconditions.

Native observations handle bounded pagination, tag peeling, hidden drafts,
starter/effective-name/digest facts and missing-digest downloads. Link metadata is
parsed or rejected; a native last-page frontier persists through short intermediate
pages. Explicit302asset redirects use a public principal and receive no repository
token. Signed URLs and arbitrary response bodies are not journal evidence. Token
helpers admit bounded repository read-route families and exact native writes;
they do not downscope remote credentials. REST reads are not atomic remote locks.

The independent kernel review closed the credential-preparation parent race and
asynchronous descriptor replacement. Independent provider review closed framing
ownership, wrong draft-state acknowledgement and next/last pagination defects.
Original reproductions and retained regressions pass. See W05-review.md for the
review scope and limitations; this is not the final release-candidate review.

Current verification:

- Strict build/root typecheck and public declaration projection pass.65actual
  declarations are emitted;4packages/9public entries are implemented with9future
  surface requirements still pending. The full seven-package product is incomplete.
- Full regression:231tests/2323assertions across36files. This includes the GitHub
  suite:29tests/526assertions, comprising28focused tests/214assertions and one
  native process test/312assertions. The independent reviewer reran28/214.
- Node22.22.2 is killed after the second asset commits, before its receipt. A fresh
  Bun1.3.14 process using the same real Git journal uploads only the remaining
  asset while earlier assets are hidden. Restored Node observes the exact set and
  publishes once; a further Bun restart sends nothing. Seven native protocol
  writes, seven dispatch starts and six accepted receipts remain honest. This
  fixture bridges logical GitHub URLs to a local HTTP peer; it is not hosted
  GitHub or a native shared-HTTP mutation witness.
- Actual Node/Bun native HTTP reads observe existing v0.3.0 release380698559 and
  commitcea8c957090d89c88838782c4d3265521f47d895. Both download checksum asset539900425
  through302→200 and verify1178bytes with SHA256
  7fe0cb16a414274bc02b4bbe5b41b96fe3b9b562593085c45aced1984a61987b, then admit the
  asset observation. W05-native-reads.json binds the worker and public facts.
- Fresh four-package Bun/npm installs pass strict declarations with optional
  platform/Apple peers absent and40Node/Bun consumer checks. W05-packed-providers.json binds the exact current archives;
  its two isolated consumers execute20runtime checks each.

The954line/56declaration legacy GitHub lifecycle is physically retired, exactly
bound to preserved25de8c0. W01/W04GitHub responsibilities and the PR31hidden-draft
regression have local successor evidence. W05-migration.json explicitly carries
real app composition, read-convergence policy and hosted mutation acceptance to
W10/Plans009/010. Prior named W03/W04 acceptance receipts remain unchanged.

Physical product is14355=9453replacement+4902legacy. W05adds1562lines:1502GitHub
and60net shared-kernel lines, against1550allocated. The7750cumulative tripwire
fails1703; W05-source-variance.json permits continued implementation with exact
source bindings and no final waiver. The full-scope forecast is12904, deficit1419
against11485; even its11807low estimate exceeds the ceiling322. Owned SPDX data
adds789lines/15973bytes separately. Original22971denominator and40line marginal
extension target are unchanged. No source-reduction target has been achieved.

Exact next action: commit/push the verified W05 cutover; then implementW06catalog
through the existing Git owner. Continue W07–W10, real native/platform/consumer
acceptance, source simplification, final independent review, appropriate registry
version/cohort selection, authorized publication and fresh published consumers.
Direct Bun Sigstore/TUF remains an independent final qualification requirement.
