# W06: Catalog rendering and conditional Git

The fifth public package implements Homebrew and Scoop through separate public
subpaths. Five source files total194 normally formatted lines:69Homebrew,
48Scoop and77shared admission. They render UTF-8 from exact owned Bundle files;
the existing Git owner constructs and publishes one commit for one or two paths.
There is no catalog release engine, journal or new dispatch permission.

The native Scoop schema requires homepage and license. The proposed Manifest
omitted both; the actual public Schema class now requires them. This necessary
API correction is generated into production declarations, not hidden in an
application default. Homebrew accepts exactly four platform/architecture cells,
Scoop exactly two. Duplicate Bundle names, substituted metadata/content, conflicting
URL hashes, unsafe native paths and invalid native identifiers reject. Rendering
binds hashes to owned metadata; native remote installation verifies actual downloads.

Ruby strings escape interpolation as well as ordinary quoting. The generated
Formula uses Homebrew's own version getter: preserve exact inferred versions,
otherwise set the declared version. No duplicate URL-version parser is maintained.
Scoop's case-insensitive nightly sentinel rejects because native install_app disables
hash verification. Executable admission excludes native shim script syntax and
wildcards. These are tested against actual native consumers, including negative
controls; Linux execution of their policies is not native Windows installation.

Current local evidence:

-15tests/397assertions across3catalog files pass. Actual Ruby4.0.6/Homebrew
  loads four selectors and inert interpolation text; actual PowerShell7.6.5 runs
  official Scoop schema/selector/installer/shim controls. Sixteen additional native
  version cells preserve intended metadata despite conflicting URL filenames.
-Eight native Git cases cover both families, SHA1/SHA256, and one/two paths.
  Git commits complete before the acknowledgement is discarded. Fresh hosts reopen
  SQLite and derive Satisfied from actual ref/tree evidence with one dispatch start,
  zero accepted receipts and no second push. Each actual committed Formula file
  loads with its corresponding native class name. Unmanaged files and empty trees
  remain unchanged.
-Homebrew tap-specific style and strict audit pass for the actual generated Formula.
-Fresh five-package Bun/npm consumers pass strict declarations with optional peers
  absent, exact archive inventory and48runtime checks,24per package manager across
  Node22.22.2 andBun1.3.14. See W06-packed-providers.json.
-Independent review closed native nightly hash bypass, shim interpolation/wildcard
  paths, reserved Ruby identifiers, mismatched native Formula fixture names and
  the final diagnostic-output harness issue. See W06-review.md.

Native tool/source/checksum pins are in test/reimplementation/catalog/native-tools.json;
`bun run setup:native-catalog` reproduces the Linux tools and verifies relevant source
hashes. Current tools live only under/tmp. No hosted catalog mutation occurred.
Full regression passes246tests/2720assertions across39files. Direct verification/seal passes281evidence files/226propositions. Independent final
accounting and topology review is closed.

W06-migration.json binds and retires both assigned legacy files:882lines and62AST
declarations. App-owned source/tag/managed-pair and forward-version correction
composition remains explicitly assigned toW10, alongside actual CLI/Action/self-release
and hosted tap/bucket/native-install duties. No legacy format or correction engine
is kept as a fallback. All69launch outcomes remain open until Plans009/010 qualify
their original native witnesses.

The live owner projection now contains only the approved T3c seven-package layout.
Rejected T1/T2/T3 projections remain at commit3b712db and are referenced from design.json.
Their historical P1 marginal evidence and zero-code observations are unchanged.
The catalog family owns one private shared helper; there is no invented cross-package
edge or duplicate helper to keep rejected topologies active. Production declarations
cover70actual files;89modules each have one forecast owner. Removed research metadata
is not product source reduction credit.

Physical product13667=9647replacement+4020legacy. W06adds194lines versus350allocated,
reducing inherited planning excess1703 to1547 against the8100cumulative tripwire.
The explicit source variance permits continued work, with no final waiver. Expected
full-scope forecast12778 still exceeds11485 by1293; low11781 also exceeds the ceiling.
Owned SPDX data789lines15973bytes is separate.22971denominator and40line marginal
target are unchanged. No complete-product source pass or release is claimed.

Next: commit/pushW06, then W07 kernel CLI prerequisite and external public composition. Continue W08–W10, complete native/source
qualification, independent candidate review, authorized publication and fresh installed
published consumers. Direct Bun Sigstore/TUF remains an independent final gate.
