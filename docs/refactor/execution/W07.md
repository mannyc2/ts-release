# W07 external provider contract — locally verified

W06 is committed and pushed as `64a5204`. This wave must prove D06-01–07
with a separately packed external provider built after the core and shared CLI,
then fresh Node/Bun consumers installed by Bun/npm, with zero subsequent changes
to core/host/CLI/first-party provider sources for the composition cases.

The kernel-owned CLI prerequisite is implemented. It accepts an explicit trusted
application module and regular JSON input file, emits the complete derived report,
and leaves authorization/provider/layer choices to the application. Native signals
abort the Effect and await scope cleanup; the bin exits130/143 after that cleanup
to release native stdio handles. Ordinary Effect logs use stderr. Native pipe,
FIFO, error redaction and delayed finalizer controls are retained. Input errors
exit1, unresolved reports exit2, all-Satisfied reports exit0.

Focused suite:15tests/88assertions; Python native-pipe controls additionally execute
13assertions per runtime. Independent review closed the logger, lifetime and
backpressured-output findings under Node22.22.2 and Bun1.3.14. Native process tests
require the documented sandbox escalation; unsupported direct Bun synchronous
logging of a single1MiB message cannot establish cooperative signal behavior.
No claim is made that arbitrary synchronous trusted application code is interruptible.

The external fixture uses only public imports: service-free versioned heterogeneous
Intent/native evidence codecs, a separate canonical-byte encoder, nested owned
artifacts, core HTTP, opaque NoReplay dispatch, native conditional Git, operation-local
credential Layers, an external M2 evaluator and a JournalStore cache decorator.
`scripts/check-packed-external.ts` builds/packs core first, then builds/packs the
external package, installs clean consumers without optional peers, and compares
frozen sources/archive identities after real native execution. The final fresh run passes four Bun/npm × Node/Bun cells, each with199native
checks and35CLI processes:796checks and140processes in total. Each cell records
12HTTPrequests/11commits and two actual conditional Git pushes. Forced journal
CAS, SIGKILL after native commits, credential rotation, exact native receipts/errors
and restarts pass. All78source bindings and both packed archives remain unchanged
after the core/CLI freeze. See W07-packed-external.json and W07-native.json; native
Git histories are retained in W07-native-history/.

Full direct verification passes255tests/2782assertions across41files, with283sealed
files and226propositions. Both material and source-accounting independent reviews
are closed (W07-review.md). Actual production projection has72source/declaration
files,89logical modules, five packages and11public entries; seven required entries
remain pending. W07retires no assigned legacy files and adds90product lines.

Physical product13,757=9,737replacement+4,020legacy; the8,200planning tripwire is
exceeded1,537 with an exact-source variance, never a final waiver. Forecast12,728
exceeds the unchanged11,485ceiling by1,243. Metadata620 and ownedSPDX789lines/
15,973bytes remain separately charged. This is not an achieved full-product reduction.

Next: commit/push this verified wave, then implement W08actual effect-build producer
adoption and Apple preparations without duplicating the owned Bundle or journal. All69 launch outcomes remain
open pending Plans009/010 certification; no release has been published. The exact
source ceiling11,485 and original baseline22,971 remain unchanged.
