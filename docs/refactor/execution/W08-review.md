# Independent W08 review

Reviewer: Sartre (`/root/review_apple_w08`), GPT-6 Astra with xhigh reasoning.
Bounded implementation, native I/O, Apple recovery and accounting reviews ran
independently of the implementer. These reviews do not certify the full release.
Exact probes and logs are retained in W08-evidence/independent/.

The artifact review reproduced regular-file to FIFO blocking and the GNU special
stdin checksum name `-`. Owned ingestion now uses no-follow/nonblocking regular
file admission; public checksum names reject stdin semantics while allowing a
private logical name `-` with a safe public alias. Exact source/snapshot identity,
mode/symlink graphs, cleanup, captured owner methods and bounded native directory
reads passed independent controls. Bun's opendir materializes a whole directory;
Bun tree hosts therefore explicitly compose the native Node bounded reader.
Native syscall traces show fixed32KiB prefetch, not a claim of maxEntries OS reads.
The shared native child lifecycle preserves Git process-group kill and awaited
cleanup. Immutable orphan CAS content grants no identity or dispatch permission.

Apple review reproduced readonly-directory cleanup failing after a successful
submission, risking loss of the returned ID. Scoped cleanup now restores private
directory permissions without following external links and awaits removal on
success, typed failure and interruption. Independent ordinary-user controls
confirm external target modes and sentinel bytes remain unchanged.

The reviewer also reproduced an asynchronous transport replacement window.
Invocation capabilities now capture before input hashing; publication Plan and
Content capture before asynchronous admission. Exact native correlation negatives,
two concurrent Ready results selecting one winner, losing-Bundle rejection,
late aliases, same-revision report and restart without repeated completion pass.
Actual tagged errors retain correspondence without wire-decoding hidden Error
properties. Missing committed submit receipts remain Inconclusive; no ID guessing
or fresh-runner dispatch is authorized by read-back.

The promised public restoreTree API was extracted from the Apple restoration path.
Independent review found that upstream Tree.publish normalizes the root to0755,
so a0555 root could commit different bytes before failing. Unsupported root modes
now reject before I/O; all published candidates are inspected against the owned
manifest before commit. Bundle admission still preserves valid source root modes.
Readonly nested files/directories, chained symlinks, deleted producer sources,
mutable aliases, corrupt reads, injected unexpected files and interruption cleanup
pass independent closure probes. This explicit upstream limitation remains part
of the public API documentation.

Projection review found eight inline type imports/exports that emit empty runtime
imports under verbatimModuleSyntax. They now use whole type declarations, and an
unused host import is removed. Independent AST analysis finds no hidden core edges
or runtime cycles. Both layout and public-surface now bind the full emission
manifest and verify all81 source/declaration owners, including private Schema
bases. An isolated mutation of Content_base.bytes String to Number makes the
actual projector reject stale internal/ArtifactModel.d.ts. Provider proposal
edges remain conservative and are labeled accordingly.

Independent retirement accounting confirms14 files,1581 physical lines and118
old declarations, with no removed promised public export. W10 source/provenance,
embedded native wheel, explicit environment/tool acquisition and platform duties
remain explicit. The measured product is13570=11131replacement+2439legacy; the
9200 planning tripwire fails1931. Forecast13483 exceeds unchanged11485 by1998.
Metadata640 and ownedSPDX789lines/15973bytes are separate. This is an implementation
planning variance, never a complete-product waiver or achieved reduction.

Final harness corrections are independently closed: native Node and Bun directory tests pass; the canonical input Bundle hash matches retained raw bundle.json and serializes correctly. Independent source/forecast enumeration confirms all counts above. Final direct execution passes11 commands and273 tests/2941 assertions. Final retained packed/journal evidence review supports local wave closure. It
verified275 then-indexed file hashes,149 concrete original copies,48 kernel source
bindings, all99 packed files against both installed consumers and current dist,
11 upstream archive identities, and exact command/check counts. The reviewer cloned
and fscked all8 retained Git bundles: successful histories match8 native facts and
two native IDs with the6-event pending prefix; lost histories contain one start,
one native submit, no receipt and two zero-call Inconclusive restarts. Final mixed
Bundle hashes/membership match owned CAS. All28 original P/Q oracle bindings remain
exact and explicitly open. The final retention index is refreshed after adding the
last verifier/external/review receipts. Native
Apple/Windows signing, notarization, clean host acceptance and the independently
reviewed complete release candidate remain mandatory Plans009/010 work.
