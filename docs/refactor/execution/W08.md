# W08 real producers and owned artifact integration

Status: **locally verified**, including final packed/native-process evidence and
independent review. All292 reviewed evidence files pass integrity/projection/ancestry/migration checks.
The checkpoint is committed and pushed as `e393dd0` on `codex/complete-hard-cut`.
Execution stopped at W08 as requested; W09/W10 require explicit user resumption.
No release is published; all69
launch outcomes remain open. This wave's local evidence cannot certify the native
macOS/Windows or complete release acceptance required by Plans009/010.

The kernel now exposes actual `effect-build` and `apple` entries. The adoption
bridge accepts finalized producer File/Tree values, captures their metadata and
content-owner capabilities, copies exact verified bytes into immutable ownership,
and returns path-free Bundle members. One `restoreTree` operation serves public
consumers and Apple reconstruction; one checksum view derives SHA256SUMS from the
final Bundle. No second artifact registry, replay machine or digest store is added.
The existing Git child lifecycle is shared with bounded directory reads.

All11 actual effect-build0.6.3 archives are retained with registry SHA-1/SHA-512
and local SHA-256 checks in [W08-upstream](W08-upstream/registry.json). Immutable
upstream source is `ef29a087baac8bdbcd90a54bb62a2dceb739dd91`; no upstream source
was modified or vendored. Strict public consumers import all42 actual upstream
entrypoints. The aligned Effect beta.107 compatibility decision, exact owned
Sentinel augmentation and peer range remain unchanged. Native tool versions and
verified digests are in [W08-native-tools.json](W08-native-tools.json).

Apple preparation uses the common Plan/Journal/executor and one authoritative
history. Six app/DMG/pkg×arm64/x64 scopes bind exact signed source identities.
Only recorded native submission IDs can drive status polling; a committed submit
without a receipt remains Inconclusive across restart. Accepted completion checks
exact input/architecture/reference/tool bindings, staples and assesses before
final adoption, and selects one terminal output Bundle. Concurrent completion
cannot replace the selected winner. A final publication Plan uses that same
journal and includes every selected artifact plus any other exact release files.

Public limitations are explicit. Bun1.3.14's opendir materializes the whole
directory, so Bun tree hosts must compose `nodeDirectoryReader(absoluteNodePath)`.
The native reader stops on the first excess yielded name, with bounded libuv
prefetch, output and time; interruption kills and awaits its process group.
Upstream0.6.3 Tree.publish normalizes its root to0755, so restoreTree rejects
other root modes before I/O. Bundle admission still preserves source root modes.
Restoration validates the candidate manifest before commit and preserves nested
modes and symlinks. Native subtype extensions are not granted signing authority
merely by adoption.

Final local integration passes through `bun run check:effect-build-integration`:18 tests/159 assertions and the packed cells below. The final kernel archive SHA-256 is
`eb038cb394139f069a0f937b72578f8f987705fae53e4210b0705ad2b7c723d9`.
The producer work is `obArg9`, Apple work `UNYB6e`. Exact receipts and all8 native
Git histories are retained under [W08-evidence](W08-evidence/retention.json).
Full direct production verification passes all11 executed commands, including
**273 tests / 2941 assertions across47 files** and another fresh external-provider
consumer run. The first integrity check stopped at the expected stale seal; after independent
evidence closure, all292 reviewed bindings pass the integrity check. See
[W08-verification.json](W08-verification.json) for exact aggregate receipts.

- Actual Bun six-target, Deno six-target and LinuxGNU NodeSEA producers:13 finalized
  executables, owned reload after producer deletion, three native GNU executions.
- Actual ZIP/tar.gz, repeat digests and independent native modes/times/listing;
  traversal/collision negatives and exact Git export-ignore, symlink, executable,
  submodule exclusion, LFS pointer and UTF-8/PAX projection.
- Pinned uv0.12.0 builds uv_build0.9.27 and poetry-core2.2.1 wheel/sdist fixtures,
  with metadata inspection and four clean install/import venvs per consumer cell.
- Actual nFPM2.47.0 creates deb/rpm/apk/Arch/MSIX from restored real executable
  bytes. Offline pinned Debian/Fedora/Alpine/Arch containers install, run and remove
  packages. Exact declared Alpine dependencies are retained. MSIX inspection on
  Linux proves ZIP/manifest bytes, leaving native Windows validation/install open.
- Actual Syft1.50.0 DirectorySubject and FileSubject produce SPDX/CycloneDX JSON;
  exact components and independent sbom-utility0.19.2 fullschema positives and
  invalid-document controls pass in offline Debian.
- Fresh Bun/npm consumers run both native Node22.22.2 and Bun1.3.14, install exact
  archives with no workspace links, check strict declarations with skipLibCheck
  false, and load portable entries with optional peers absent. Four producer
  cells each execute195 checks:780 total,52 built executables,16 native package
  installs and16 clean Python environments.
- Four packed Apple cells use real bare Git journals and fresh Node/Bun processes,
  with8 SIGKILL points before/after receipt persistence. A mixed final Bundle has
  two selected apps, two actual post-staple TARs and an untouched Linux executable.
  Native tar members, Linux --version and GNU checksums are verified after reload.
  The driver executes48 commands/120 checks; four installed consumers each add8
  internal checks. Apple submit/info/staple/assessment are protocol doubles.

The previous packed receipts are retained as historical bytes, never relabeled
as final. The first kernel SHA was53551b5bdfb935209b2fd5ce53cd8ab4792ce0cdcec9ae82f4f6b195ee41ec26
before restoreTree; the second was5469a587c1249edcacc3145c73279a126a214f5e0390a18df57ff2d8b023ecbd
before final type-only import corrections. Final evidence retention identifies
its own exact pack and source hashes. Earlier package receipts omitted a nonexistent
inputBundle.id; current receipts explicitly hash encodeBundle(inputBundle).
Historical receipts remain unchanged.

[Independent review](W08-review.md) closed material FIFO, checksum stdin,
readonly cleanup, invocation-capture, restoration-before-commit, runtime-edge
and private-Schema declaration-binding findings. A final integration failure was
reproduced as a test PID-marker creation/write race; the corrected observer waits
for a valid positive PID and100 native Bun repetitions pass. This correction
changes the oracle, not production process behavior. The failed receipt is retained.

[W08-outcome-evidence.json](W08-outcome-evidence.json) preserves all28 exact original
P/Q scopes and native pass witnesses, maps the local implementations and identifies
remaining certification. It does not close the launch ledger. Native Apple build,
signing, notarization, second-runner credentials, stapler/Gatekeeper, Windows SDK/
SignTool/timestamps/install, remaining executable targets, clean tool acquisition
and final release source/application binding remain required. Pure-Python fixtures
do not replace selected native embedded CLI wheel acceptance. Direct Bun Sigstore,
hosted trust/mutations, Action, self-release and agent product remain later work.

[Migration](W08-migration.json) physically retires all14 assigned files:1581 lines
and118 declarations. Exact original hashes/declarations and successors are recorded.
The remaining2439 legacy lines are unshipped later-wave donors. Source/commit/tree
and dirty-input binding, app manifest and embedded native Python delivery, explicit
tool/environment/cache acquisition, and native platform obligations carry explicitly
toW10. Deleted seccomp code is not represented as a capability still provided by
the new application; its selected environment guarantees still need real evidence.

Actual normally formatted product is **13570=11131replacement+2439legacy**,
187 fewer current physical lines thanW07. Replacement grows1394 against1000
allocated, leaving a **1931-line** excess over the9200 planning tripwire. The
[exact planning variance](W08-source-variance.json) permits continued implementation
without a final waiver. There are81 actual source/declaration files and92 logical
module owners. The full emission manifest binds private bases as well as public
entries; no promised public export was dropped. Forecast13483 exceeds unchanged
11485 by1998 (low12723/high14423). Metadata640 and ownedSPDX789lines/15973bytes
are charged separately; product plus data14359, plus metadata14999. Tests,
fixtures, generated output and research/tooling remain separate. Selected scope
is incomplete: no achieved full-product source reduction is claimed.
