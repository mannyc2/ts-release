# W09 MCP and OpenAI outcomes

Status: **locally verified**, with exact protocol, package, migration and packed
consumer evidence. This is a local implementation checkpoint, not hosted
acceptance or publication. All69 launch outcomes remain open.

The new `@mannyc1/ts-release-mcp` package owns one MCP Registry vertical. It
admits the official 2025-12-11 server manifest shape, all five selected package
kinds, all three transports, immutable package coordinates, namespace-bound
token or GitHub OIDC authority, and one exact `POST /v0.1/publish`. Prepared
request ownership rederives the body, byte length and SHA-256 digest. A lost or
unreadable response remains unknown; continuation performs an exact version
read-back and never converts discovery into permission to resend.

The new `@mannyc1/ts-release-openai` package owns deterministic skills-only
plugin trees. It validates exact owned content, modes, paths, canonical
`plugin.json`, one complete `SKILL.md` tree and the absence of token-shaped
material. Marketplace construction preserves unrelated entries, replaces only
the selected plugin and produces exact canonical bytes for the existing
conditional-Git owner. Submission output is strictly a human handoff: it binds
an exact Bundle member, logo, marketplace entry, listing, starter prompts,
exactly five positive tests, exactly three negative tests and explicit
attestations. There is no portal transport and no publication-success state.

The focused production suite passes **9 tests / 41 assertions**. The complete
reimplementation suite passes **282 tests / 2,982 assertions across 49 files**.
The current packed external matrix freezes all97 package/source bindings before
building and runs Bun/npm installs under native Node22.22.2 and Bun1.3.14. Its
four cells pass796 checks and140 CLI/native processes with zero source edits;
the kernel archive remains
`eb038cb394139f069a0f937b72578f8f987705fae53e4210b0705ad2b7c723d9`.
Strict build, production declarations, import boundaries, package exports and
migration checks pass. See [W09-verification.json](W09-verification.json).

[Migration](W09-migration.json) physically retires all three assigned agent
implementation files:348 lines and49 top-level declarations. Their mutable ZIP,
cache and installation behavior is not retained as a second plugin system. The
agent application metadata and user-facing skill remain for W10 ownership
reconciliation; they do not supply another OpenAI implementation.

Actual normally formatted product is
**14,874=12,783 replacement+2,091 legacy**. W09 adds1,652 replacement lines
against1,800 allocated and retires348 legacy lines. The exact
[planning variance](W09-source-variance.json) is1,783 lines over the11,000 W09
tripwire and permits continued implementation only. It is not a final waiver.
The mixed actual/future forecast is13,225 (low13,065/high13,465), still1,740
above the unchanged11,485 ceiling. Metadata732 lines and owned SPDX data789
lines/15,973 bytes are separately charged.

Hosted MCP Registry publication/discovery, marketplace Git mutation, human
OpenAI portal review and publication, credentialed trust and final candidate
qualification remain Plans009/010 work. No remote mutation or release occurred.
W10 must complete Action/CLI/self-release and agent ownership, retire the final
2,091 legacy lines, prove packed hosts and simplify the complete selected product
below the unchanged ceiling.
