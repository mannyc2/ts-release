# W10 CLI, Action and self-release convergence

Status: **locally qualified candidate**. W01-W10 implementation, migration and
the unchanged source ceiling are complete on the sequential hard-cut branch.
This is an unpublished local candidate, not hosted acceptance or independent
Plan010 certification. All 69 selected launch outcomes remain open.

The shared `@mannyc1/ts-release` package now owns the public `ts-release` bin,
the scoped application contract and both explicit Node and Bun hosts. A trusted
command-line application path supplies the complete Bundle, immutable Plan and
host; Plan or Journal data cannot choose executable code. The same admitted
Bundle/Plan pair is used before execution and for the final report. SIGINT and
SIGTERM abort the scoped run, await cleanup and map to conventional process exit
codes without introducing a second lifecycle engine.

`apps/action` is a thin Node action over that same application boundary. It
admits the application only after lexical and real-path containment under
`GITHUB_WORKSPACE`, accepts one JSON input, writes only the exact plan ID and
journal revision outputs, and emits no caught private diagnostic. The checked-in
launcher is built from this source for `node24`; local packed qualification runs
the copied Action with native Node22.22.2 because hosted node24 remains Plan009
work. Bun and npm fresh installs each execute a first run and a fresh-runner
continuation with one total native send, no workspace symlink and no optional
peer installation. The final launcher SHA-256 is
`937cab71641211ecd1fa3a027b7f8c7a14f10bc367370e8bd09b2fa0332fe719`.

`apps/self-release` constructs the intended release through public package
contracts. It binds the exact source commit/tree, owned Bundle bytes and Plan ID;
requires all seven npm packages, four platform-tagged PyPI wheels, one GitHub
source tag, exact Homebrew and Scoop members, the MCP publication operation and
the validated OpenAI plugin/marketplace path. The returned run is deliberately
non-mutating: `authorize:false`, `observe:false`, `maxDispatches:0`, unavailable
network reads and a rejecting transport. The test therefore proves the complete
intended-byte plan without turning branch completion into release authority.
The agent distribution is now one valid `.codex-plugin/plugin.json` package with
the maintained release skill; the retired mutable installer/manifests are not a
second plugin owner.

[Migration](W10-migration.json) retires all 18 assigned TypeScript files: 2,067
lines and 152 top-level declarations across the old CLI, Action and root host.
The old 24-line Action metadata is also retired, while the selected 22-line
metadata remains charged. No inherited legacy source remains.

The exact [source result](W10-source-result.json) is
**11,484 product lines = 11,484 replacement + 0 legacy**, one line below the
unchanged 11,485 ceiling against the original 22,971 denominator. Metadata
is 694 lines; owned product data remains separately visible at 789 lines/15,973
bytes. Tests, fixtures, tooling and generated delivery remain in their own
recorded lanes.

Local qualification includes:

- Focused Action/self-release: 5 tests, 58 assertions across 2 files.
- Complete reimplementation suite: 287 tests, 3,040 assertions across 51 files.
- Packed producers: four Bun/npm by Node/Bun cells, 195 checks each (780 total),
  36 commands, strict public declarations and no workspace links.
- Packed Apple recovery: four cells, 48 processes, 120 driver checks, 32
  installed checks and 8 actual SIGKILL boundaries over real Git journals.
- Packed external composition: four cells, 796 checks and 140 CLI/native
  processes with 100 frozen source/package bindings and zero source edits.
- Packed Action: Bun and npm installs on native Node22.22.2, one send per
  first-run/fresh-runner pair and absent optional peers.
- Strict packed all-seven-package provider/catalog/AI consumers under Bun and
  npm: seven exact archives, 30 runtime outcomes per manager and declaration
  checking with `types:[]`, `skipLibCheck:false` and absent optional peers.

The final kernel archive SHA-256 is
`a8b8d9dafe4efe2df0ff13f642cb0cdfdf8da40b0e7a1b2bfe48e1ba0947ca32`.
Production projection contains 90 source/declaration owners, 93 logical modules
and 336 public symbols. Build, declarations, architecture, imports, exports,
migration and source checks pass. Exact retained receipts and hashes are in
[W10-verification.json](W10-verification.json).

No registry, repository, marketplace, portal, signing service or release tag
was mutated. Hosted destinations, credentialed trust, native macOS/Windows,
direct Bun Sigstore/TUF, complete launch evidence and an independent cold review
remain Plans009/010 work and must not be inferred from this local candidate.
