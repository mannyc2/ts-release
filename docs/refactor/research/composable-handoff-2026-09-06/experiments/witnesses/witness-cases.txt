a provider built against another contract version is rejected at Host verification before any store or provider effect
two physical kernel copies of the same contract interoperate at the data level (the hazard is version skew, not duplication itself)
${candidate}: cache hit — one ordinary release costs one authoritative read instead of many
${candidate}: stale cached revision cannot mint a permit; the authoritative CAS refuses and the runner re-reads
${candidate}: two concurrent runners with private caches over one journal produce one CAS winner and one send
${candidate}: ambiguous append invalidates the cache; the live runner reconciles by an authoritative read-back; a fresh runner never resends
${candidate}: a lying cache (truncated or tampered history) cannot turn into a send; a stale report is visibly stale
independent releases keep independent histories in one store
${candidate}: a committed send whose receipt fails strict decoding is journaled as a bounded diagnostic; no resend; later observation satisfies
store laws hold for MemoryJournal, SqliteJournal and the caching decorator over both
store laws reject a last-writer-wins store (it would mint permits without CAS)
${candidate}: a preparation selects one satisfied output through the ordinary observe path; a competing different selection is refused by the same CAS loop; publications may be re-observed
