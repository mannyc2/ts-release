# Plan 229 — Recovery history decision

Input-Commit: 3a5b7cef4437c5f59bc547481535ebbc83cf437f
Result-Commit: SELF
Evidence-Commit: SELF
Status: DECIDED / OPTIONAL-EVIDENCE FOR INSTALLED PROVIDERS / NO JOURNAL ADDED
Outcome: PROVIDER-TRUTH-RECOVERY / NO-CORRECTNESS-CRITICAL-HISTORY

Date: 2026-08-12

Commit convention: `SELF` means this completed implementation decision and its
deterministic handoff are intentionally co-committed in candidate result X. It
does not name Plan 233 certificate Y or supply live-provider evidence. The
sibling provider-profile document is generated byte-for-byte from the runtime
registry and therefore intentionally carries no separately edited commit
metadata.

## Decision

The installed npm and GitHub publication profiles use
`historyRequirement: "optional-evidence"`. Their exact remote observations,
together with the verified prepared bundle, reconstruct the safe next action.
No correctness-critical `ReleaseHistory` service or mutation journal is added.
Losing local history must not block a fresh exact observation, and retaining a
journal must never authorize mutation replay.

This conclusion is limited to the installed npm and GitHub publication
adapters. It does not certify future PyPI or catalog modules, and it does not
admit an automated correction adapter. npm deprecation and GitHub release
amendment remain operator proposals because no conditional write bound to an
observed generation was proven.

## Fault evidence

The decision follows the executable cases below rather than a preference for
or against event sourcing.

| Fault or competing actor | Executable evidence | Result relevant to history |
| --- | --- | --- |
| npm response lost after provider acceptance | `test/protocol/npm/npm-provider-protocol.test.ts` — “response loss never republishes and exact reread is the only convergence proof” | One publish is recorded; the attempt is `OutcomeUnknown`; the exact package/version integrity and dist-tag reread proves equivalence. No journal fact is used as provider truth. |
| GitHub response lost after release creation | `test/protocol/github/github-protocol.test.ts` — “preserves unknown transport outcomes and converges by exact reobservation” | Exactly one POST occurs; the attempt is `OutcomeUnknown`; exact tag, release metadata, and asset-set observation proves convergence. |
| two npm actors race on the same absent version | `test/protocol/npm/npm-provider-protocol.test.ts` — “two actors observe version absence, one wins publish, and the loser reobserves without replay” | Both actors hold a version-absence decision. The winner publishes, the stale actor receives a started/nonzero result, and the loser reobserves exact integrity and tag equivalence. Neither actor replays its mutation. |
| two GitHub actors race on the same absent coordinate | `test/protocol/github/github-protocol.test.ts` — “two actors observe absence, one wins create, and the loser reobserves without replay” | Both actors hold an authenticated absence decision. One create applies, the stale create receives an uncertain result, and the loser reobserves exact equivalence. No actor repeats its mutation; successful shared mutation count stays one. |
| a deleted npm coordinate appears absent | `test/protocol/npm/npm-provider-protocol.test.ts` — “delete-like absence never makes a consumed npm package version reusable” | A trusted actor may receive an absence/create decision, but provider rejection plus continued absence remains unknown/pending. The profile retains `consumed-after-delete`; absence never becomes reuse proof. |
| confirming reads remain hidden or inconclusive | `test/publication/recovery-coordinator.test.ts` — visibility-pending convergence, inconclusive convergence, retry exhaustion, and budget cases | Only post-mutation reads are retried. Exhaustion yields `UncertainSubject` with the complete ordered trace; it does not manufacture success, absence, or replay permission. |
| pre-mutation read is inconclusive or conclusive conflict/absence | `test/publication/recovery-coordinator.test.ts` — pre-mutation inconclusive, present-different, and authoritatively-absent cases | Pre-mutation uncertainty fails closed, and conclusive facts stop immediately. Provider decision and mutation counts remain unchanged by the reread loop. |
| GitHub asset enumeration is truncated | `test/protocol/github/github-protocol.test.ts` — full pagination and later-page failure cases | Set equality requires all pages. A failed later page is `Inconclusive`; a partial set or search result cannot become history-backed proof. |
| two correction actors propose different amendments | `test/correction/plan229-authored-correction.test.ts` — “two correction actors get distinct canonical proposals and neither can dispatch” | Each exact prepared subject produces a canonical proposal, but the installed correction capability is empty. Neither actor can turn an earlier read or a journal entry into write authority. |
| an ordinary npm publish observes a deprecated version | `test/publication/plan224-conservative-provider-subjects.test.ts` — “ordinary npm publication preserves an existing deprecation while proving package bytes equivalent” | Exact tarball integrity and dist-tag equality converge without publish authority or mutation, so the old publication path neither clears the warning nor treats correction as rollback. |

## Outcome-unknown rule

A mutation whose dispatch may have begun is never made safe merely by logging
it. For the current observable providers, the coordinator can advance only
after a new conclusive provider observation. If observations exhaust, the
release remains `UncertainSubject`; a new runner must observe again and must not
blindly replay the mutation.

If a future admitted adapter performs a mutation that cannot be reconstructed
by exact remote observation, its profile must change to
`durable-cas-required` before dispatch is reachable. That requirement means a
shared durable compare-and-set claim store spanning all runners. A local
append-only file, process lock, CI workspace, or ephemeral cache is
insufficient because another runner cannot atomically observe and claim the
same dispatch right.

For such a future adapter, the state transition is one-way:

1. atomically claim the exact prepared subject before dispatch;
2. once dispatch may have started, record the claim as terminal
   `OutcomeUnknown` if no conclusive result is available;
3. never let lease expiry, timeout, runner death, lock loss, or a new claimant
   turn `OutcomeUnknown` into `Absent` or retry permission;
4. advance only from new conclusive provider evidence or an explicit typed
   operator decision.

An operator decision is a new canonical fact about what to do next. It is not
proof of the original provider outcome and must be reported separately from
provider observation.

## Journal boundary

Optional audit evidence may record sanitized observations, dispatch-start,
provider result, final observation, and typed operator decisions. It earns no
authority:

- graph and prepared bytes remain authoritative only through the canonical
  prepared bundle;
- journal loss does not change a safe next action for an installed observable
  provider;
- journal presence never proves remote equivalence, absence, or exactly-once
  execution;
- a journal must not carry graph bytes as an alternate authority;
- no event, lease, timeout, or runner identity permits blind replay.

Because no installed profile requires durable CAS and the fault cases recover
from provider truth or stop uncertain, adding a history service now would add
state without reducing the current correctness state space. The canonical
subject/profile gate prevents a future adapter from silently advertising a
different history requirement. A future `durable-cas-required` module must
also add an availability/durability gate and shared-store fault tests before
its mutation becomes reachable.

## Verification boundary

All evidence above is local and uses versioned, stateful protocol doubles. No
live npm package, GitHub repository, release, tag, asset, credential, or
external journal was read or mutated for this decision.
