In this repo (the Effect v4 source; we pin effect@4.0.0-beta.83): my TypeScript code hand-rolls the following mechanic ({families}):

{mechanic}

```ts
{snippet}
```

Does this repo already provide APIs that model this mechanic directly?
- For each relevant API that actually exists here: give the exact export as `Module.export`, the defining source file path (packages/effect/src/...), and a one-line signature or minimal usage sketch.
- If a near-miss API name you considered does not exist in this repo, say "does not exist" explicitly for it.
- If this repo's own source solves the same problem the way my snippet does, say so and cite one example file that does it.
- Only name APIs that exist in this repo's source. If nothing fits, answer exactly: nothing fits.
