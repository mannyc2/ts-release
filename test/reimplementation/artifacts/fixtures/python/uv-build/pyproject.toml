[build-system]
requires = ["uv_build==0.9.27"]
build-backend = "uv_build"

[project]
name = "effect-build-uv-fixture"
version = "1.0.0"
requires-python = ">=3.11"
description = "uv_build acceptance fixture"
